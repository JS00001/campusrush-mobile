name: Deploy (Production)
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: yarn

      # Check whether app.config.js "version" is updated from last commit/build AND the package.json "version" is updated from last commit/build
      - name: Ensure version update
        run: |
          if [[ $(git diff HEAD^ HEAD app.config.js | grep -c '"version"') -eq 0 ]]; then
            echo "app.config.js version not updated"
            exit 1
          fi

          if [[ $(git diff HEAD^ HEAD package.json | grep -c '"version"') -eq 0 ]]; then
            echo "package.json version not updated"
            exit 1
          fi

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.PRODUCTION_EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: yarn install

      - name: 🚀 Build app
        run: eas build --non-interactive --platform ios

      - name: 🚀 Publish app
        run: eas submit --non-interactive --platform ios --latest

      # Report status to slack channel via webhook
      - name: ✅ Report Status
        if: always()
        uses: ravsamhq/notify-slack-action@v1
        with:
          icon_success: ":white_check_mark:"
          icon_failure: ":x:"
          token: ${{secrets.GITHUB_TOKEN}}
          status: ${{ job.status }}
          notification_title: ""
          message_format: "{emoji} *{workflow}* {status_message} in <{repo_url}|{repo}>\n\n*Commit URL:* <${{github.event.head_commit.url}}|${{github.event.head_commit.id}}>\n*Commit Message:* ${{github.event.head_commit.message}}\n*Commit Author:* <https://github.com/${{github.event.head_commit.author.name}}|${{github.event.head_commit.author.name}}>\n*Build URL:* <${{github.event.repository.url}}/actions/runs/${{github.run_id}}|${{github.run_id}}>"
          footer: "Linked Repo <{repo_url}|{repo}>"
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL_PROD }}
